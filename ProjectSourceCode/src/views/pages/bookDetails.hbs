<!DOCTYPE html>
<html>
<head>
  <title>{{book.title}}</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <div class="card shadow">
      <div class="row g-0">
        <div class="col-md-4">
          <img src="{{book.coverLink}}" class="img-fluid rounded-start" alt="Cover image of {{book.title}}">
        </div>
        <div class="col-md-8">
          <div class="card-body">
            <h2 class="card-title">{{book.title}}</h2>
            <p class="card-text"><strong>Author(s):</strong> {{book.author}}</p>
            <p class="card-text"><strong>Year of Release:</strong> {{book.year}}</p>
            <p class="card-text"><strong>ISBN:</strong> {{book.isbn}}</p>
            <p class="card-text"><strong>Publisher:</strong> {{book.publisher}}</p>
            <p class="card-text"><strong>Subject(s):</strong> {{book.subject}}</p>
            <form method="POST" action="/assign-fav">
              <input type="hidden" name="isbn" value="{{book.isbn}}">
              <button type="submit" class="btn {{#if book.isFavorited}}btn-danger{{else}}btn-primary{{/if}}">
                {{#if book.isFavorited}}Remove from Favorites{{else}}Add to Favorites{{/if}}
              </button>
            </form>
            <a href="/searchtest?query={{book.title}}" class="btn btn-secondary mt-3">Back to Search Results</a>
          </div>
        </div>
      </div>
    </div>
  </div>
  <!-- Bootstrap JS Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
